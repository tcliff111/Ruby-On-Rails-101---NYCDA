<h1 id="hey">Hey</h1>
<br />
<% (1..10).each do |i| %>
<%= "#{i}. #{@string}" %>
<br />
<% end %>

<p>Input all items on item list separated by commas:</p>

<%= form_tag("/page") do %>
	<%= text_field_tag(:items) %>
	<%= submit_tag("Submit") %>
<% end %>